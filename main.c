#include <stdio.h>
#include "headers/TokenType.h"
#include "headers/Token.h"

int main()
{// bananalyzer entry point
// read file per line
//
    tokenValue(AND);
    printf("%s",toString());
    return 0;
}